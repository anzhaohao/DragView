# DragView (dsh-drag-file-preview) bundle patch.
#
# The host half (package root) registers the resolve/copy/config HTTP routes
# and the settings section; the browser half (dsh.client) is discovered
# automatically through the package's dsh.client declaration + exports["./client"].

- insert:
    - id: drag-file
      name: dsh-drag-file-preview
